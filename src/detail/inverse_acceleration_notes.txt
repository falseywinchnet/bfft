Inverse acceleration decisions
==============================

2026-06-21

Local sync:
- The current branch is `work` and has no configured upstream remote/tracking branch.
- `git fetch --all --prune` had nothing to fetch.
- `git pull --ff-only` could not update because there is no tracking information for `work`.

Primary decision:
- Start with the near-free scheduling win before deeper per-ISA kernel rewrites.
- Build `INV_RES_SCHEDULE` from `FWD_RES_SCHEDULE` by reversing the forward residue schedule and appending the final inverse binomial that undoes the initial forward `binomial_oop`.
- Keep the record type shared with the forward scheduler for now. The operation kind identifies the forward primitive; inverse dispatch swaps each kind to its inverse kernel.

Why this is first:
- It removes the recursive inverse traversal and replaces it with the same linear switch shape already used by the forward path.
- It keeps all q==8, q==4, q==2, and spine operations in one baked stream, exposing predictable control flow and sequential op metadata to SSE/NEON, AVX2, and AVX-512 builds.
- It does not change transform math, layout, or public APIs, so correctness risk is lower than starting with new fused inverse scatter or d4 inverse codelets.

ISA notes:
- SSE/NEON benefit immediately from removing recursive calls and from the existing 128-bit `norm_q_inv` loop.
- AVX2 now uses the scheduled inverse as well.  After the double-precision d4 inverse fusion, q==8 leaves stay register-resident in both recursive and scheduled runners, and direct inverse benchmarking on an AVX2 host showed the schedule paying for its flat dispatch.
- AVX-512 keeps the existing recursive path until the d4 fusion and schedule tradeoff are benchmarked on a native AVX-512 target.

Completed:
1. [done] Fused inverse d4 codelet for f32 q==8 (`codelet_d4_inv_f32`): two inverse d3 leaves + inverse q=8 norm in one function, values stay in registers.  Eliminates 8 loads + 8 stores per q==8 block.  Wired into both the schedule runner and the recursive path.
4. [done] Assembly inspection of the SSE2/NEON schedule loop identified that all inverse twiddle recombinations used `V4F_ADD(V4F_MUL, V4F_MUL)` instead of `V4F_MADD`/`V4F_MSUB`.  Fixed in `norm_q_inv_f32`, `norm2_inv_fused_f32`, `codelet_d3_tw_res_inv_f32`, and the double V2 equivalents.  NEON FMA count in the inverse schedule runner went from 8 to 38 (matching the forward), total instructions from 1162 to 961.

5. [done] Fused d4 inverse codelet for double-precision AVX2 (`codelet_d4_inv`): uses `__m256d` intrinsics for the BRUUN_LEVEL>=2 path, falls back to three separate calls otherwise.  Wired into both `rec_inv_res` q==8 and `run_inv_residue_schedule` FWD_OP_CODELET_Q8.  AVX2 assembly: 2212 instructions, 55 FMAs, 169 spills + 161 reloads (expected — 16 YMM registers vs 8 live output vectors plus twiddles).  Spills hit L1 stack; still a net win over unfused path's 32-double store/reload through the data array on large transforms.

Follow-up candidates:
2. Fuse native/standard gather with the inverse binomial tail to remove one complete pass over N for inverse-from-complex APIs.
3. Add f32/scalar lane-doubling equivalents where the inverse leaf still falls back to generic inverse-norm ladders.

AVX2 schedule gate:
- Rechecked the scheduled inverse on Intel Xeon Platinum 8370C with `-O3 -mavx2 -mfma -mno-avx512*`.
- The benchmark now prints a direct `Inv_ns` column so inverse-only changes are visible instead of inferred from round-trip timing.
- The d4 fusion was correctness-checked on AVX2 and is fast enough to enable the flat inverse schedule for BRUUN_LEVEL 2.
